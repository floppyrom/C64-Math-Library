2026-09-25: Seek movement, stable entries 47-54. MATH_SEEK8_* (x,y 0..255) and MATH_SEEK16_* (16-bit x) move up to eight objects to a target pixel along the exact Bresenham line and land exactly on it, at a Q8.8 speed along the major axis or along the path. Per frame 87.1 / 71.0 / 66.0 cycles (SEEK8 STEP / STEP_INT / STEP1, all profiles); SEEK8 init 337 cycles on V2-V4. No new ZP, no REU, no MATH_INIT; placed in free islands inside the existing regions. API is now 54 entries; nothing else changed. Following Repose's review on CSDb. See docs/SEEK_DDA.md.

2026-09-21: Smaller normalize and ATAN2. Normalize saves 290 B in V1/V2/V5 and 18 B in V3/V4; means improve to 160.150080 and 156.661198 cycles. Fast ATAN2 saves 4 code bytes with identical timings and outputs. No per-case slowdowns in the comparison corpus. Stock normalize outputs change within the existing accuracy bounds; REU outputs and images are unchanged. See docs/SIZE_OPTIMIZATION_2026-09-21.md.

2026-09-21: Faster vector normalization. V1/V2/V5: 161.332256 cycles; V3/V4: 157.552684 cycles. Four scratch bytes, same public precision bounds. Stock code/tables grow; V1/V2/V5 PRG load changes to $1000. See docs/VEC2_NORMALIZE_Q8_8.md.

2026-09-20 - STANDALONE ROUTINE SOURCES / TYPED NAMES
- 245 callable profile routines now have standalone readable ASM mirrors.
- Typed names encode operand/result geometry; legacy MATH_* ABI remains unchanged.
- Tables/manifests updated and cross-checked; measured benchmark values unchanged.

2026-09-20 DIVISION FAMILY REFRESH
- Re-evaluated the complete UDIV/SDIV + UMOD/SMOD family across V1-V5.
- Repose q0-counter UDIV24 integrated profile-by-profile; V1/V2/V5 get faster CPU UDIV8 while V3/V4 keep their faster REU 8-bit planes.
- Native signed 8/16/24/32 division refreshed; signed paths remain executable-disjoint from unsigned engines.
- Same-corpus old/new audit: 23 public division-family paths per profile, zero timing regressions.
- Final validation: 822,050 unsigned + 364,533 signed division/modulo calls, zero errors.
- Custom Pareto RAM points: 9377/9950/10893/9581/11079 B at 31/36/60/147/176 ZP; V5 HYBRID_CODE is now $A000-$BDFF (7680 B).
- Reference/alternate, hybrid, Pareto, deterministic, package and 205-check release audit pass; no fresh external ACME run.

2026-09-20 FIVE-PROFILE MULTIPLICATION REFRESH
- Re-evaluated UMUL8/16/24/32 and SMUL8/16/24/32 across V1-V5, including READY and fixed-shift derivatives, using the Repose signed-quadrant and recent quarter-square/carry work.
- Direct signed SMUL8 is now 67.992188 cycles exhaustive in every profile. V1/V5 select low-ZP FAST17 SMUL16; V2/V3/V4 keep their faster 116-ZP practical native SMUL16.
- FAST24 signed composition is selected in every profile; V1/V5 also select the private FAST24 unsigned producer. Mixed-call-safe FAST31/V29 UMUL32 and SMUL32 are selected in all five profiles; UMUL32 measures 712.235367 cycles on the common refresh corpus.
- Public API addresses are unchanged. Private multiply cores extend the resident payload end to $CF96; no selected fixed profile reserves persistent hardware-stack space. V3/V4 Turbo entry geometry remains intact.
- Custom Pareto now treats the refreshed V1 baseline correctly: redundant initialized-UMUL32 donor retired, legacy 5-ZP pack accelerates UMUL8 only, and the 24-ZP pack imports the current FAST24 signed core. Equal-weight extra-RAM points: 9377/9950/10893/9581/11079 B at 31/36/60/147/176 ZP.
- Validation: 264,999 signed multiply calls, 415,078 cross-profile refresh benchmark calls, 17,196 Turbo relocation calls, 12 Pareto reference/alternate builds (55,068 common-API calls), signed/unsigned executable zero-overlap, deterministic source/hybrid rebuilds, and release audit PASS_WITH_ACME_NOT_RUN.

2026-09-18 VEC2 NORMALIZE — STABLE 46TH API ENTRY
- Added MATH_VEC2_NORMALIZE_Q8_8 at reference $5E39: signed Q8.8 x/y -> signed Q1.15 unit vector; inputs preserved; C=1 only for zero vector.
- Final means: V1 198.770271 / V2 189.260317 / V3 170.058633 / V4 170.058633 / V5 198.770271 cycles.
- Common precision contract <=0.3621 deg / <=202 LSB; corrected full-domain proof <=0.360856382 deg / <=200 LSB.
- V1/V5 keep the 31-byte ZP contract. V3/V4 use a 32 KiB direct ratio-index table in the upper half of relocatable REU_TURBO16_BANK, with no extra REU bank.
- Stable surface is now 46 entries in every fixed profile and generated Custom Pareto build. Fresh reference/alternate V1-V4 validation: 46/46, 4,589 calls per map; Pareto matrix: 55,068 common-API calls.
- Normalize kernels are now available as standalone native ASM in every profile under resident/vector/native; V1-V4 include them directly in canonical source builds and validation byte-compares isolated vs integrated code on both maps.

2026-09-17 ATAN2 FAST TIERS + GAME AUDIT
- ATAN2 exhaustive 65,536-vector result: V1 50.441345 cycles mean (30-53), V2/V3 46.953064 (30-48), V4 exact 48 fixed, V5 46.953064; stock implementations use 0 extra ZP and V1-V3/V5 stay within 1 phase unit.
- V1 keeps the compact 512-byte table point; V2/V3 use the 1280-byte fast tier. V5 imports the fast tier through 3 previously unused 256-byte pages with collision guards.
- Custom Pareto adds independent atan2_fast pack: 0 ZP / 768 B. At that revision the default extra-RAM points were 6021/6611/7371/6207/7557 B; superseded by the 2026-09-20 multiplication refresh.
- V5 direct-import validation: 144,246 cases including exhaustive ATAN2 result/cycle parity. Pareto deep parity: 75,644 cases including exhaustive ATAN2, plus exhaustive UMOD8 and stress/ZP checks.
- Added source audit for Steel Ranger, Wolf64 and Quake64: keep existing discrete/direct-angle paths where they are cheaper; use ATAN2 for genuine dynamic vector-to-angle work.

2026-09-14 UMUL RECORD UPGRADE
- UMUL16: qualified 17-ZP record-derived resident kernel.
- UMUL24: certified 24-ZP reverse/carry record kernel.
- V3/V4 Turbo32: stack-free 135-ZP ram135 compromise, legal ZP origins $02-$79.
- Added consolidated cycles/code/ZP/stack table for every shipped routine/profile.

Native signed refresh:
- Every SMUL/SDIV public path now owns a native signed executable kernel; no corresponding unsigned executable engine is entered.
- Profile ZP budgets and public ABI are unchanged; affected signed multiply finalizers are 3 cycles shorter where safely compacted.
- Same-corpus check: all newly split SMUL paths are 3 cycles faster; the already-native V2-V4 SMUL16 is unchanged; SDIV32_32 is faster in every profile.
- Added zero-overlap graph checks plus dedicated multiply/divide arithmetic regressions.

2026-09-14 EARLIER SIGNED SOURCE/TAXONOMY CLEANUP (SUPERSEDED)
- This intermediate documentation-only taxonomy was replaced later the same day by the all-native signed kernel refresh at the top of this file.
- Its 264,999-call multiply validation is retained and now complemented by dedicated division and executable-ownership validation.

C64 MATH LIBRARY — CUSTOM PARETO BUILDER — 2026-09-07

New stock-C64 build-time optimizer: tell it the total ZP budget (plus optional RAM budget, init policy and hot-routine weights) and it selects the fastest certified compatible V1/V2 implementation mix with no runtime dispatcher. Same 45-entry API. Default breakpoints: 31/36/60/147/176/221 ZP. 31 ZP + zero extra RAM reproduces V1 byte-for-byte; 31 ZP + optional init reproduces V5; 221 ZP selects full V2.

Validation: 12 reference/alternate generated builds, 50,064 common-API calls; 75,644 direct V2 cycle-parity cases including exhaustive ATAN2; exhaustive UMOD8; 50,144 SMUL16 parity cases; 25,000 mixed iterations; 10,000 ZP-guard iterations; deterministic rebuilds and 12/12 invalid resource-map tests.

C64 MATH LIBRARY — V5 HYBRID LOW-ZP — 2026-09-06

New stock-C64 V5 profile: V1's 31-byte normal ZP budget with selected faster V2 kernels, while retaining the same 45-entry API. Direct gains include UDIV16 14.23%, UDIV32/16 11.39%, COS8 20.69% and SINCOS8 20.51%. Reference + alternate maps pass all 45 entries (4,172 calls each), plus 144,246 direct-import cases including exhaustive ATAN2 result/cycle parity, exhaustive UMOD8/trig checks, ZP-confinement stress, cold-load/no-init tests, deterministic rebuild and config rejection tests.

The project is now presented as the C64 Math Library; “API” refers to the stable callable interface.

CSDb CHANGELOG — DOCUMENTED LEAN FINAL

Packaging/manual follow-up:
* added plain-English Turbo16/Turbo32 explanation + BEGIN/CALL/END diagram
* explains REU as overlay storage/DMA, not the arithmetic engine
* generated build_source/ trees no longer shipped; recreated on demand
* removed superseded slow-ISQRT32 candidate evidence and unreferenced legacy intermediates
* corrected stale segment-manifest paths
* code, ABI, Turbo overlays and deployable REU images unchanged

* Complete USER_MANUAL.md added
* All 45 stable calls documented with inputs/outputs/status
* Full V3/V4 REU + relocatable Turbo16/Turbo32 usage documented
* V4 QS16 small-batch mode documented
* Custom assembly-time relocation walkthrough and safety rules added
* IRQ/reentrancy, C64 banking, REU-image matching and troubleshooting documented
* Corrected VICE REU filenames in the REU guide
* Code and ABI are byte-for-byte unchanged from Turbo FINAL

C64 MATH API V1-V4 — TURBO RELOCATION FINAL — 2026-09-06

- same 45-entry stable signed/unsigned/game-math API
- V3/V4 add 6 Turbo lifecycle entries: 16/32 BEGIN/CALL/END
- Turbo16 + Turbo32 executable-ZP origins now selectable at build time
- Turbo16 + Turbo32 REU storage banks now selectable at build time
- overlay binaries assembled from canonical source; no runtime relocation
- reference PRGs + V3/V4 REU images reproduce byte-for-byte
- alternate Turbo16: $3E-$AE -> $40-$B0
- alternate Turbo32: $0A-$FA -> $06-$F6
- V3 turbo banks 4/5 -> 0/1; V4 turbo banks 4/5 -> $28/$29
- 17,164 Turbo product calls + 32 BEGIN/END calls PASS (17,196 Turbo API calls total)
- exact ZP restore + second-batch reuse verified
- reference/alternate cycle vectors identical: zero relocation penalty
- ACME 0.97 independently reproduces resident + overlay builds
- config safety suite expanded to 27 cases
- endpoint sweep: T16 $02/$8F + T32 $02/$0F, V3 banks $06/$07, V4 $FE/$FF: 3,556 products PASS

Fast exact ISQRT32 retained:
V1 1378.900969 / V2 1198.619613 / V3 1197.859322 / V4 1046.622518 cycles

Profiles:
V1 Balanced / stock C64 low-ZP
V2 Pareto-Fast / stock C64 speed-space knee
V3 REU 512K + relocatable Turbo overlays
V4 REU 16M + QS16 + relocatable Turbo overlays
2026-09-14 SOURCE-PUBLICATION FIX
- Added exact per-routine native signed SMUL/SDIV source mirrors to every V1-V5 profile, including V1 where multiply/native previously contained only a README.
- Added byte-for-byte publication validation; resident PRG/REU payloads are unchanged.
